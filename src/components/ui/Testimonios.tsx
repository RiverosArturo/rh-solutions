import React from 'react'

export const Testimonios = () => {
    return (
        <>
            {/* Clientes/Testimonios */}
            {/* <motion.section
        id="clientes"
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1 }}
        viewport={{ once: true }}
        transition={{ duration: 0.6 }}
        className="py-20 px-6 bg-gray-50"
      >
        <h2 className="text-3xl font-bold text-center mb-12">Clientes Satisfechos</h2>
        <div className="max-w-3xl mx-auto text-center text-gray-700 italic">
          "RH Solutions transformó por completo nuestra operación interna con una app que automatiza el 80% de nuestro trabajo diario. ¡Recomendado al 100%!"
          <div className="mt-4 font-semibold">– Cliente Feliz</div>
        </div>
      </motion.section> */}
        </>
    )
}
